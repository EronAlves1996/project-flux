Today I see somethings of internals of popular environment variables libraries.
THe parsing from string to type safe values can be a tricky thing, so these things should be managed in a library.
Actually, use as many ready solutions as possibles to avoid the tricky things and focus on everything that max your business value most.
