I am slowling building the system using interfaces, first using simpler approachs, like in memory storage, random data generator, etc.
After the system is built, I can cleanly swap the implementations using real implementations and can extend the system with newer implementations.
