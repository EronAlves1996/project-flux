Learn more about the value of third party libraries, encompassing validation to typesafe value mangagement.
We make the precautions to not break this in production using validation and fallback values. 
But, the application cant be up if passing invalid values to env, so documentation is a thing very important
