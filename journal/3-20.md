I learned more about currency generation.
Implemented some form of ingestion using stream approach to collect things and accumulate them in a batch.
This can break in production if the source system doesnt produces any batch or if the memory is low, or the data is malformed accordingly with the accepted schema, leading to schema changes.
We have to remember the rules and approach for schema evolution to support backward and frontward compatibility.
