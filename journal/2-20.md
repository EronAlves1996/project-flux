Don't learned anything valuable, only thinking in good ways to generate better financial data.
Reinforced the learning from before, to properly encapsulating implementations.
Because this is a directed mock, it only executes a single piece of code without dynamic data, production usage would introduce dynamic data, that will break in production.
